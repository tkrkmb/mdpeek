# mdpeek 計画

Neovimで編集中のMarkdownを、GitHub風の見た目で別ウィンドウにプレビューする個人用ツール。ソースはGitHubで公開するが、使うのは自分だけという前提で作る。

## 要件

- 対象：macOS / Linux、Neovim 0.10以上
- 構成：Rust + Tauri v2、フロントエンドはTypeScript（Vite）
- 入力が200ms止まるたびに、プレビューを自動で更新する（保存は不要）
- スクロールを双方向に同期する
  - Neovimのカーソル位置にプレビューが追従する
  - プレビューをCmd+クリック（macOS）／Ctrl+クリック（Linux）すると、Neovimがその行へジャンプする
- Mermaid、数式、ダークモード（`T` キーで system / light / dark を切り替え）に対応する
- Markdown内の生HTMLは描画しない

## 仕組み

```
Neovim（Luaプラグイン） ⇄ RPC ⇄ Rust（comrak） ⇄ IPC ⇄ 画面（TypeScript）
```

- アプリは、NeovimのRPCソケットに接続する。独自のサーバーは立てない
- 本文は、Neovimのバッファから直接送る（ファイルは読まない）
- 常に最新の本文を描画するので、行のずれを検出して同期を止めるような仕組みは不要になる
- 本文には版番号を付け、古い本文や古い描画結果が後から適用されないようにする

## 使い方

- `:MdPeek`：現在のバッファのプレビューを開く。すでに開いている場合は、表示対象をこのバッファに切り替える
- `:MdPeekClose`：プレビューを閉じる。Neovimを終了した場合も、プレビューは閉じる

## 進め方

AIに1段階ずつ実装させ、段階ごとに動作を確認してからコミットする。

1. 接続と基本表示
2. 自動更新
3. Mermaid・数式・画像・リンク・テーマ
4. スクロール同期
5. READMEの作成と、実機での確認

## 割り切り

- 長い段落や表の内部では、スクロール同期は概算になる
- 配布用パッケージは作らない（`cargo tauri build --no-bundle` で実行ファイルだけ生成する）
