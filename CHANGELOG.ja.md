# 変更履歴

[English](CHANGELOG.md) | 日本語

MdSightの利用者から見える変更を、版ごとに記録します。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に倣います。

新しい版を出すときは、「未リリース」の見出しを `## [vX.Y.Z] - YYYY-MM-DD` に書き換えてからタグを付けます。GitHubのReleaseの説明は、英語版（`CHANGELOG.md`）の同じ見出しの節から作られます。

## [未リリース]

### 追加

- YAMLのフロントマターを、GitHubと同じく先頭に表で出すようにしました。これまでは区切り線と見出しに化けていました。YAMLとして読めないときは、コードブロックで出します。
- 脚注（`[^1]`）とアラート（`> [!NOTE]`、`[!TIP]`、`[!IMPORTANT]`、`[!WARNING]`、`[!CAUTION]`）を描画するようにしました。
- 言語名の付いたコードブロックに、ライト／ダークそれぞれの配色で色を付けるようにしました。
- Neovimのプレビューが、プレビューしているウィンドウでの別のMarkdownファイルへの移動（`gf`、`:e`、`Ctrl-O`、`:b`など）に追従するようにしました。このとき窓は前に出ません。ほかのウィンドウでの移動には、これまでどおり追従しません。

### 変更

- プレビューしているバッファを`:bd`で消しても、そのウィンドウが残っていれば、プレビューを閉じないようにしました。そのウィンドウに次に表示されたMarkdownファイルに追従します。

### 修正

- Neovimのプレビューでリンクを辿ったときや戻る／進むのときに、文書は開いたのにエラーが出て、リンクの`#見出し`へも移動しないことがある問題を直しました。

## [v0.9.0] - 2026-09-25

### 変更

- 名前をMdPeekからMdSightに変えました。`mdpeek`という名前を、ほかのツールがすでに使っているためです。リポジトリ、実行ファイル、アーカイブ、Neovimのモジュールとコマンドが、すべて新しい名前になります。更新するときは、READMEの手順で入れ直し、設定を`require("mdsight").setup({ bin = ... })`（新しい実行ファイルのパス）に変え、`:MdSight`／`:MdSightClose`を使ってください。古い`~/.local/share/nvim/site/pack/mdpeek`は消してください。`T`キーで選んだテーマは、1回だけsystemに戻ります。

## [v0.8.0] - 2026-09-25

### 追加

- READMEと変更履歴の英語版を用意した。日本語版は`README.ja.md`と`CHANGELOG.ja.md`に移した。

### 変更

- アプリのアイコンを新しくした。プレビューのテーマ（ライト／ダーク）に合わせて、ライト用とダーク用が切り替わる。macOSでは、リリース版でもDockにアイコンが出るようになった。

### 修正

- リンクや戻る／進むで別の文書に移ったとき、前の文書で読んでいた位置ではなく、先頭（`#見出し`付きのリンクならその見出し）から表示するようにした。

## [v0.7.0] - 2026-09-25

### 追加

- MIT Licenseで公開した（`LICENSE`）。リリースのアーカイブに、同梱したライブラリのライセンス表示（`THIRD_PARTY_NOTICES.md`）を入れるようにした。
- `mdpeek --version`で、版を表示できるようにした。

### 変更

- Releaseに添付するアーカイブの名前から版を外した（`mdpeek-universal-macos.tar.gz`、`mdpeek-x86_64-linux.tar.gz`）。READMEの導入手順は、常に最新の版を取得する。

## [v0.6.0] - 2026-09-25

### 追加

- ウィンドウの一番上に、表示中のファイルのパスを出すようにした。長いパスは左側を省略し、マウスを乗せている間だけ流れて全体が見える。
- `mdpeek ファイル名`で、同じファイルを表示しているウィンドウが既にあれば、新しく開かずにそのウィンドウを前面に出すようにした。
- Neovimで別のバッファに`:MdPeek`したとき、プレビューが他のウィンドウに隠れていれば前に出すようにした。入力はNeovimに残る。

### 変更

- 戻る／進むのボタンを、本文の右下からパスの帯の左端に移した。本文の上に重ならなくなった。
- ウィンドウ内の文言と、ターミナルへの出力を英語にした。
- ウィンドウのタイトルで、Neovimと連携している窓（`MdPeek — Linked to Neovim`）と、ファイルを開いただけの読み取り専用の窓（`MdPeek — Read Only`）を見分けられるようにした。
- `mdpeek ファイル名` は、ウィンドウをバックグラウンドで開いてすぐに終わるようにした。ターミナルを占有したまま動かしたいときは `--foreground` を付ける。
- ファイルを読み直せない・監視できないといった問題を、ウィンドウの下端に表示するようにした。読み直せるようになると消える。
- リンクを開けなかったとき、戻る／進むに失敗したときに、その理由をウィンドウの下端に数秒だけ表示するようにした（これまでは何も起きなかった）。

## [v0.5.0] - 2026-09-24

### 追加

- `mdpeek ファイル名` で、Neovimを使わずに単体で起動できるようにした。ファイルを外部のエディタで保存すると、表示が自動で更新される。
- 相対パスの `.md`／`.markdown` リンクをクリックすると、その文書を開くようにした。`#見出し` 付きのリンクでは、その見出しへ移動する。
- リンクで開いた文書を戻る／進むで辿れるようにした。右下のボタン、キーボード（macOSは `Cmd+[` / `Cmd+]`、Linuxは `Alt+←` / `Alt+→`）、トラックパッドの2本指横スワイプで操作できる。
- Neovimからプレビューしているときにリンクを開くと、Neovim側の対象ウィンドウも同じファイルに切り替わるようにした。

### 変更

- ソースからビルドするのに必要なRustの版を、実際の依存関係に合わせて 1.88.0 以上とした。

## [v0.4.0] - 2026-09-24

### 変更

- 実行ファイルとNeovimプラグインを1つのアーカイブで配布するようにした。Neovimのパッケージ用ディレクトリに展開するだけで導入できる。
- READMEを、導入からビルドまで順に追える構成にした。

## [v0.3.0] - 2026-09-21

### 修正

- テーマの切り替えが、`T` キーを1回押しただけでは反映されないことがあった問題を修正した。

## [v0.2.0] - 2026-09-21

### 変更

- 本文を窓の幅いっぱいに表示するようにした。

### 修正

- プレビューの準備ができる前に送られたカーソル行が失われる問題を修正した。
- `:MdPeek` で対象を切り替えたときに、プレビューの位置がカーソルに合わない問題を修正した。

## [v0.1.0] - 2026-09-21

最初の版。

### 追加

- `:MdPeek` で、Neovimで編集中のMarkdownをGitHub風の表示でプレビューする。保存しなくても、入力が止まると表示が更新される。
- `:MdPeekClose` でプレビューを閉じる。
- Mermaidの図、KaTeXの数式、画像（相対パスと `https:`）の表示。
- `T` キーによる system／light／dark のテーマ切り替え。
- Neovimのカーソルに合わせたスクロールと、修飾キー付きクリックによるNeovimの該当行への移動。
- macOS（Intel／Apple Silicon）とLinux（x86_64）向けの実行ファイルを、タグからReleaseに添付するCI。
