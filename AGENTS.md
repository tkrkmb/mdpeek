# MdPeek 実装指示書

この文書に書かれたことだけを実装する。書かれていないことは実装しない。判断に迷ったら、推測で進めずに質問する。なお、質問は選択形式とすること。

## 概要

Neovimのバッファ内容を、GitHub風のHTMLで単一ウィンドウにプレビューするTauriアプリと、それを操作するNeovimプラグイン。利用者は作者1人。

## 前提

- OS：macOS / Linux
- Neovim：0.10以上（`vim.system`、`vim.uv` を使ってよい）
- Rust + Tauri v2、nvim-rs（tokio）、comrak
- フロントエンド：TypeScript + Vite。フレームワークは使わない
- 同梱ライブラリ：github-markdown-css、mermaid、katex（npm経由でバンドルする）

## リポジトリ構成

```
mdpeek/
├── .github/workflows/ # リリース用のビルド
├── src-tauri/        # Rust
├── ui/               # TypeScript + Vite
├── lua/mdpeek/       # Neovimプラグイン（リポジトリのルートをプラグインとして読み込めるようにする）
├── AGENTS.md
├── HANDOFF.md        # 引き継ぎ資料（毎回上書き）
└── README.md
```

## 作業の進め方

### DO

- 下の「段階」を1つずつ実装する。1段階を終えたら**必ず作業を止め**、次の3点だけを簡潔に報告して指示を待つ
  - 実装した内容
  - 確認した内容と、確認の手順（実行するコマンドと、Neovimでの操作）。実行できなかった確認があれば、それも書く
  - 未実装の部分と、既知の問題
- 確認は、変更に関係する範囲から始める（例：Luaだけ変えたならLuaの動作だけ）。そのうえで、報告の前には必ず `cargo build` と `npm run build` を通す
- 読むのは、その段階の作業に必要なファイルだけにする。探すときは、対象を絞った検索を使う
- 変更は、その段階の仕様を満たす最小限にする
- 依存関係は、実際に使うものだけを追加する。バージョンは固定し、ロックファイルもコミットする
- 作業を終えるときは、途中から再開できるように、引き継ぎ資料を `HANDOFF.md` に上書きで残す。次の項目だけを、各1〜3行で書く
  - 現在の段階と、その状態（完了／作業中）
  - 次にやること
  - 未解決の問題と、確認できていないこと
  - 動作確認に使うコマンド
  - 次の作業で最初に読むべきファイル（数個まで）
- `HANDOFF.md` は、読むだけで作業を再開できる最小限の内容にする。全体で50行以内に収める
- `HANDOFF.md` は `private` ブランチに置く。読み書きの前後で `git checkout private`／`git checkout main` で切り替える

### DO NOT

- 報告の前に、次の段階へ進まない
- この文書にない機能、設定項目、コマンドを追加しない
- 仕様を変えて済ませない。仕様どおりに実装できない場合は、止まって質問する
- 関係のない整理やリファクタリングをしない。作業を妨げない問題を見つけたら、直さずに報告に書く
- リポジトリ全体の走査、変更していないファイルやログの読み直し、生成物・キャッシュ（`target/`、`node_modules/`、`dist/`）やロックファイルの読み込みを、必要なしにしない
- 配布用パッケージ、署名、自動更新は作らない。CIは、下の「CI」に書いたものだけにする
- Windows向けのコードや分岐は書かない
- `HANDOFF.md` に、コードの全文、設計の経緯、過去の作業履歴を書かない。追記して積み上げず、必ず上書きする

## Git

### DO

- 利用者が段階の報告を確認して承認したら、その段階の変更をコミットする
- 編集やコミットの前に、`git status` と関係する差分を確認する
- コミットは、1つの目的ごとに分ける。機能、設定、テスト、ドキュメントは、分けられる範囲で別のコミットにする
- ステージするのは、関係するファイル（または変更箇所）だけにする。コミットの前に、ステージした差分を確認する
- コミットメッセージは英語で、Conventional Commitsの形式 `<type>(<scope>): <why>` にする
  - 件名は1行（なるべく72文字以内）で、「何を変えたか」ではなく「なぜ必要か」を書く
  - type は小文字（`feat`、`fix`、`refactor`、`docs`、`test`、`chore` など）。scope は `nvim`、`rpc`、`render`、`ui`、`sync` などを使い、意味のあるものがなければ省略する
  - 本文は、理由が1行で書けないときだけ付け、英語で理由を書く
- `HANDOFF.md` と `PLAN.md` は `private` ブランチでのみ追跡する。`main` には置かない
- push は `git push origin main` だけにする（`--all` は使わない）

例：

```text
feat(nvim): let the preview follow the buffer without saving
fix(sync): stop the preview from bouncing back after a jump
chore(ui): bundle mermaid so the preview works offline
```

### DO NOT

- 利用者の承認の前にコミットしない
- 自分が変更していない作業中の変更を、消したり、コミットに含めたりしない
- 履歴を書き換えない（rebase、amend、force push などは、指示があったときだけ行う）
- 秘密情報、環境固有のパスや識別子、一時的な生成物をコミットしない
- `update README` や `fix bug` のような曖昧な件名にしない
- `private` ブランチを push しない
- `HANDOFF.md` と `PLAN.md` を `main` にコミットしない

## 全体の仕様

### Neovimプラグイン（Lua）

DO

- `require("mdpeek").setup({ bin = "<実行ファイルのパス>" })` で設定する。設定項目は `bin` だけにする
- 次の2つのコマンドを提供する
  - `:MdPeek`：現在のバッファとウィンドウを対象にする。プレビューが開いていなければアプリを起動し、開いていれば対象を切り替える（本文もすぐに送る）
  - `:MdPeekClose`：アプリに終了を通知する。応答がなければプロセスを終了させる。Lua側の状態、autocmd、タイマーもすべて解放する
- 対象にできるバッファを「`buftype` が空で、ファイル名があり、`filetype` が `markdown` のもの」に限る。それ以外なら理由を `vim.notify` で表示して、何もしない
- RPCソケットには `vim.v.servername` を使う。空であれば `vim.fn.serverstart()` で作成する
- アプリは `vim.system` を使い、引数の配列（`--nvim <socket> --token <起動トークン>`）で起動する。プロセスが終了したら、Lua側の状態を解放する
- 対象を設定するたびに「対象世代」を1増やす
- 本文を送るたびに「版」を1増やす
- 本文の送信：対象バッファの `TextChanged` / `TextChangedI` を受けて、200msのデバウンス後に全行を送る。対象を設定したときは、デバウンスせずにすぐ送る
- カーソル位置の送信：対象ウィンドウでの `CursorMoved` / `CursorMovedI` を受けて、50ms間隔のスロットル（最新の値を送る）で行番号を送る。直前に送った行と同じなら送らない
- 対象を設定したときは、スロットルを待たずに、その時点のカーソル行もすぐ送る
- 対象ウィンドウが閉じたら（`WinClosed`）、カーソル位置の送信を止める
- 対象バッファが削除されたら（`BufWipeout` / `BufDelete`）、`:MdPeekClose` と同じ処理を行う
- ジャンプ要求を受けたら、1回の関数呼び出しの中で次のすべてを検証してから、`nvim_win_set_cursor(winid, {line, 0})` を実行する。検証に失敗したら、何もしない
  - 対象世代と版が、現在の値と一致すること
  - 対象ウィンドウが有効で、対象バッファを表示していること
  - 行番号が、バッファの行数の範囲内にあること
- ジャンプ先の行が折り畳まれていたら、`normal! zv` で開く
- ジャンプを実行したら、「直前に送った行」をジャンプ先の行に設定する（この結果として起きる `CursorMoved` を送らないため）
- リンクを辿る要求（`open`）を受けたら、1回の関数呼び出しの中で対象世代と版が現在の値と一致することを検証してから、対象ウィンドウで指定されたファイルを開く。開く前に、いまの対象のautocmdを外す。開いた後のバッファが「対象にできるバッファ」の条件を満たしていれば、それを新しい対象にする（対象世代を1増やし、本文とカーソル行をすぐ送る）。開けなかった場合や条件を満たさない場合は、理由を `vim.notify` で表示し、元の対象のままにする

DO NOT

- ファイルを読まない。`BufWritePost` やファイル監視も使わない（ただし、リンクを辿る要求を受けたときは、対象ウィンドウで指定されたファイルを開いてよい）
- `nvim_win_set_cursor` にウィンドウ番号 `0` を渡さない
- 他のバッファに移動したとき、表示対象を自動で切り替えない
- アプリ側から任意のLuaコードを実行させる口を作らない。アプリから呼べるのは、`require("mdpeek.rpc")` が公開する固定の関数（`register`、`jump`、`open`）だけにする

### RPC（NeovimとRustの間）

DO

- アプリは起動したらソケットに接続し、`nvim_get_api_info()` で自分のチャネルIDを取得する。その後、`nvim_exec_lua('return require("mdpeek.rpc").register(...)', {token, chan})` を呼ぶ
- `register` はトークンを検証してチャネルを記録し、初期状態（対象世代、版、本文の行配列、文書の絶対パス、カーソル行）を返す
- Neovimからアプリへの通知は、`vim.rpcnotify(chan, name, payload)` で送る。通知の種類は次の3つに限る
  - `mdpeek_content`：`{gen, version, path, lines}`
  - `mdpeek_cursor`：`{gen, line}`
  - `mdpeek_close`：`{}`
- アプリからNeovimへのジャンプは、`nvim_exec_lua('return require("mdpeek.rpc").jump(...)', {gen, version, line})` で行う
- アプリからNeovimへのファイルを開く要求は、`nvim_exec_lua('return require("mdpeek.rpc").open(...)', {gen, version, path})` で行う。`path` は絶対パス。戻り値の真偽で、開けたかどうかをアプリに伝える
- RPCが切断されたら、アプリを終了する

DO NOT

- HTTPサーバー、WebSocket、TCPでの待ち受けを作らない
- RPCの受信処理の中で、描画が終わるのを待たない。受け取ったらキューに渡してすぐ返す

### Rust

DO

- comrakで次の拡張を有効にする：table、strikethrough、tasklist、autolink、`math_dollars`、`header_ids`（プレフィックスは空文字）
- `render.sourcepos = true` を有効にする
- `render.unsafe_` は `false` のままにする（生HTMLは描画しない）
- ```` ```mermaid ```` のブロックはcomrakで通常どおり出力し、フロントエンドで処理する
- 世代と版が最新の本文だけをHTMLに変換し、Tauriのイベントでフロントエンドへ送る。フロントエンドが最新の状態を取得するためのコマンドも用意する
- 画像のパスを解決するコマンドを用意する。入力は相対パスと版で、手順は次のとおり
  - 文書のディレクトリを基準にパスを解決する（パーセントエンコードのデコードも行う）
  - シンボリックリンクを実体のパスに解決する
  - ファイルが存在し、拡張子が png、jpg、jpeg、gif、webp、svg のいずれかである場合だけ、そのファイル単体をassetプロトコルのスコープに追加して、絶対パスを返す
- 画像パスの解決には、`cargo test` で動くテストを書く
- 起動引数が `--nvim <socket> --token <token>` ならNeovim連携モード、単一の位置引数（ファイルパス）ならスタンドアローンモードとして起動する。それ以外の引数の組み合わせはエラーを表示して終了する
- スタンドアローンモードでは、指定されたファイルを読み、GFMのHTMLに変換して表示する。ファイルが存在しないか、拡張子が `md`／`markdown` でなければ、エラーを表示して終了する
- スタンドアローンモードでは、開いているファイルを監視し、変更されたら読み直して表示を更新する。世代は「開くファイルが変わるたび」、版は「読み直すたび」に1増やす
- 文書内の相対パスで、拡張子が `md`／`markdown`（末尾に `#見出し` が付いていてもよい）のリンクを開く要求を受けたら、いまの文書のディレクトリを基準にパスを解決する（手順は画像の解決と同じ：パーセントデコード、シンボリックリンクの解決、存在の確認）。解決できたら、そのファイルを新しい文書として開く。Neovim連携モードでは、Rustから `rpc.open` を呼んでNeovim側にも同じファイルを開かせ、対象を切り替えさせる
- リンクで開いた文書の履歴を、戻る／進むで辿れるようにする。戻る・進むとも、開き直す手順は上と同じ（スタンドアローンモードならファイルを読み直す、Neovim連携モードなら `rpc.open` を呼ぶ）。Neovim連携モードで `rpc.open` が失敗を返したら、履歴を動かさない
- 戻る／進むそれぞれを辿れるかどうかを、フロントエンドが取得できるコマンドを用意する
- Neovim連携モードで、`rpc.open` 以外の理由（`:MdPeek` による対象の切り替えなど）で対象世代が変わったら、リンクの履歴を空にする
- フロントエンドが起動時のモード（Neovim連携／スタンドアローン）を取得できるコマンドを用意する

DO NOT

- ディレクトリ単位でassetプロトコルのスコープを許可しない
- Neovim連携モードでは、Markdownのファイルを読みに行かない（本文はNeovimから受け取る）

### フロントエンド（TypeScript）

DO

- 本文は `.markdown-body` 要素の中身だけを差し替える。ページ全体の再読み込みはしない
- 本文の幅は窓いっぱいに使う。左右の余白は同じにする
- 受け取った版が、表示中の版より古ければ破棄する。非同期の処理（Mermaid、画像、KaTeX）は、完了した時点で版を確認し、古ければDOMに反映しない
- 差し替える前に「画面の上端付近にあるブロックのソース行と、画面内でのオフセット」を記録し、差し替えた後にそこへ戻す
- Mermaid：`startOnLoad: false`、`securityLevel: "strict"` を指定する。図の元のコードとテーマの組をキーにして、SVGをキャッシュする。構文エラーはそのブロック内にだけ表示し、他の部分の描画は続ける
- KaTeX：`[data-math-style]` の要素だけを描画する。`trust: false`、`throwOnError: false` を指定する。数式の元のコードをキーにして、結果をキャッシュする
- 画像：相対パスは、Rustのコマンドで解決してから `convertFileSrc` でURLに変換する。`https:` の画像はそのまま表示する。それ以外のスキームの画像は表示せず、代替テキストを表示する
- リンク：`#` で始まるリンクは、ページ内でスクロールする。`http:` / `https:` のリンクは、tauri-plugin-openerで既定のブラウザを開く。相対パスで、拡張子が `md`／`markdown`（末尾に `#見出し` が付いていてもよい）のリンクは、Rustのコマンドでファイルを開かせる。開けたら、`#見出し` があればその見出しへ、なければ先頭へ移動する。それ以外のリンクは何もしない。どの場合もWebView自体は遷移させない
- 「戻る」「進む」のキー（macOSは `Cmd+[` / `Cmd+]`、Linuxは `Alt+←` / `Alt+→`）で、リンクで開いた履歴を辿る
- 「戻る」「進む」のボタンを、本文の左上に常時重ねて表示する。辿れない方は無効にする。有効/無効は、起動時と、リンクを開いた後、戻る/進むを行った後に更新する
- トラックパッドの2本指横スワイプでも、戻る／進むを行う。クリックされた位置の祖先に、横スクロールできる要素（表、コードブロック、Mermaid図など）があれば、その要素が既にスクロールの端に達しているときだけ、戻る／進むとして扱う（端に達していなければ、要素自体の横スクロールを優先する）
- 位置表：`[data-sourcepos]` の付いた要素のうち、`.markdown-body` の直下にある要素と、リストの `li` だけを対象にする。開始行の順に並べ、`{startLine, endLine, element}` の配列として保持する
- 位置表は、描画の完了時、画像の読み込み完了（`load` / `error`）時、Mermaidの描画完了時、ウィンドウサイズの変更時に作り直す
- カーソルへの追従：その行を含むブロック（なければ直前のブロック）を探す。画面内に見えていなければ、ブロックの上端が画面の上から1/3の位置に来るように、アニメーションなしでスクロールする。見えていれば、スクロールしない
- 描画を待っている間に届いたカーソル位置は、最新のものだけを保持し、描画が完了してから適用する
- 修飾クリック（macOSは `metaKey`、Linuxは `ctrlKey`）を受けたら、クリックされた要素に最も近い祖先で、位置表に含まれる要素を探し、その開始行でジャンプを要求する。見つからなければ、縦方向で最も近いブロックを使う。修飾クリックのときは、リンクの通常動作を止める
- 描画を待っている間（受け取った版が表示中の版より新しいとき）は、修飾クリックを無視する
- テーマ：`T` キーで system → light → dark の順に切り替える。選択は `localStorage` に保存する。system のときだけ、OSの設定変更に追従する。テーマを変えたら、Mermaidの図を描き直す
- 位置表の構築と、行からブロックを探す処理には、vitestでテストを書く

DO NOT

- ブロック内での補間スクロールは実装しない
- CDNからは何も読み込まない（外部から読み込んでよいのは `https:` の画像だけ）
- 通常のクリックでジャンプさせない

### CI

DO

- ワークフローは `.github/workflows/release.yml` の1本だけにする
- `v` で始まるタグを押したときに動かす。手動でも実行できるようにする
- Linux（Ubuntu、x86_64）とmacOS（IntelとApple Siliconの両方で動く1つの実行ファイル）を作る
- どちらも、フロントエンドをビルドしてから `cargo build --release` を実行する
- できた実行ファイルを `tar.gz` にまとめ、タグから作るGitHubのReleaseに添付する

DO NOT

- Windows向けのビルドはしない
- 署名や公証、自動更新は行わない
- GitHubが公式に出しているもの以外のActionは使わない

### Tauri の設定

DO

- ウィンドウは1枚だけにする。タイトルは `MdPeek` にする
- CSPは、同梱したスクリプトとスタイル、Tauri IPC、assetプロトコル、`https:` の画像、`data:` のフォントと画像に限って許可する。KaTeXとMermaidの動作に必要なインラインスタイルは許可する
- `beforeBuildCommand` にフロントエンドのビルドを登録する

DO NOT

- `unsafe-eval` を許可しない（Mermaidの動作に必要になった場合は、止まって報告する）

## 段階

各段階の最後に、上の「作業の進め方」に従って止まる。

### 段階1：接続と基本表示

- 実装するもの：`setup`、`:MdPeek`、`:MdPeekClose`、アプリの起動、RPCでの登録、初期本文の表示（GFMとgithub-markdown-cssのライトテーマ）、終了処理
- 完了の条件
  - `:MdPeek` で本文が表示される
  - `:MdPeek` を連打しても、アプリは2つ起動しない
  - `:MdPeekClose`、プレビューを閉じる操作、Neovimの終了のいずれでも、両側の状態が解放され、その後で再度 `:MdPeek` できる

### 段階2：自動更新

- 実装するもの：`TextChanged` / `TextChangedI` による本文の送信、版による破棄、スクロール位置の保持、`:MdPeek` による対象の切り替え
- 完了の条件
  - 入力を止めてから約200ms後に、表示が更新される
  - 更新の前後で、読んでいた位置が保たれる
  - 別のバッファで `:MdPeek` すると、表示がそのバッファに切り替わる

### 段階3：描画機能

- 実装するもの：Mermaid、KaTeX、キャッシュ、画像、リンク、テーマの切り替え、CSP
- 完了の条件
  - 図や数式に構文エラーがあっても、他の本文は表示される
  - 図を含む文書で文字を入力しても、変更していない図がちらつかない
  - 生HTML、許可していないスキームの画像、スコープ外のパスの画像は表示されない
  - リンクをクリックしても、WebViewは遷移しない

### 段階4：スクロール同期

- 実装するもの：位置表、カーソルへの追従、修飾クリックによるジャンプ、往復の防止
- 完了の条件
  - 同じバッファを2つの分割ウィンドウで開いたとき、対象のウィンドウだけが動く
  - ジャンプした後に、プレビューが勝手に動かない
  - 画像や図の読み込みが終わった後も、同期の位置がずれない
  - 空の文書、文書の末尾、空行でも、エラーにならない

### 段階5：仕上げ

- 実装するもの：README（導入方法、`setup` の書き方、コマンド、ビルドのコマンド `cargo tauri build --no-bundle`、既知の制限）
- 完了の条件：READMEの手順どおりに進めれば、ビルドと起動ができる

### 段階6a：スタンドアローン表示とファイル監視

- 実装するもの：起動引数によるモード分岐、`mdpeek <file>` でのファイルの読み込みと表示、ファイルの変更監視と再描画
- 完了の条件
  - `mdpeek README.md` のようにファイルを指定して起動すると、本文が表示される
  - 存在しないファイルや、拡張子が `md`／`markdown` でないファイルを指定すると、エラーを表示して終了する
  - 別のエディタでファイルを上書き保存すると、表示が自動で更新される

### 段階6b：相対リンクでの文書間移動

- 実装するもの：相対パスの `.md`／`.markdown` リンクでの文書の切り替え、戻る／進むの履歴、リンク遷移後の画像パスの解決
- 完了の条件
  - 相対パスの `.md` リンクをクリックすると、そのファイルが表示される。`#見出し` が付いていれば、その見出しへ移動する
  - 戻る／進むのキーで、開いた履歴を辿れる
  - リンク先の文書にある相対パスの画像が表示される

### 段階6c：Neovim連携モードでのリンク追従

- 実装するもの：`rpc.open` の追加、プレビューでのリンク遷移をNeovim側の対象ウィンドウに反映する処理
- 完了の条件
  - `:MdPeek` で表示中に、プレビューで相対 `.md` リンクをクリックすると、対象ウィンドウでそのファイルが開き、対象が切り替わる
  - 対象バッファに保存されていない変更があり開けなかった場合は、通知が表示され、対象もプレビューも元のままになる
  - 戻る／進むのキーで、Neovim側の対象も一緒に切り替わる
